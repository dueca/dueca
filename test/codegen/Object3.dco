(Header "
        Give me additional header code")

(Type double)
(Type std::vector<double> "#include <vector>")

(Stream Object3
  (Option msgpack)
  (std::vector<double> a (Default 1.0) (DefaultSize 1))
)
