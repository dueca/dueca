#!/usr/bin/bash

@XVFB@ @DISPLAY@ -screen 0 @DISPLAYSIZE@x24 &
XVFBPID=$!
export DISPLAY=@DISPLAY@

sleep 1
@WINDOWMANAGER@ &
sleep 1
timeout --kill-after=3 10s \
       @Python_EXECUTABLE@ -c "import pynput"
RESULT=$?
sleep 1

kill ${XVFBPID}
exit ${RESULT}


